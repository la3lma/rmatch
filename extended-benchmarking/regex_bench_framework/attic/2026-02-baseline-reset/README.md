# Attic Snapshot (2026-02 Baseline Reset)

This directory contains non-canonical artifacts moved out of the active benchmark lane.

Purpose:
- reduce noise in day-to-day benchmarking
- preserve old outputs and experiments for later reference

Note:
- legacy tracked historical assets elsewhere in the repository are intentionally left intact for provenance.
- canonical execution should use the new plan and matrix files under `docs/` and `test_matrix/`.
