# Charts

This directory now intentionally contains only the current GCP-comparable snapshot charts used by the top-level README:

- `gcp_e2_10k_runtime_seconds.png`
- `gcp_e2_10k_relative_x_vs_winner.png`
- `gcp_e2_10k_throughput_mib_s.png`

Legacy non-GCP chart images were moved to:

- `archive/2026-03-attic-consolidation/charts/legacy-non-gcp/`

If you regenerate old chart sets locally, do not commit them back into `charts/` unless we explicitly decide to re-adopt that layout.
